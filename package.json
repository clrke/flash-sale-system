{
  "name": "flash-sale-system",
  "version": "1.0.0",
  "private": true,
  "description": "High-throughput flash sale system for a single limited-stock product",
  "workspaces": [
    "packages/backend",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @flash-sale/backend",
    "dev:web": "npm run dev --workspace @flash-sale/frontend",
    "build": "npm run build --workspace @flash-sale/backend && npm run build --workspace @flash-sale/frontend",
    "test": "npm run test --workspace @flash-sale/backend",
    "stress": "npm run stress --workspace @flash-sale/backend",
    "typecheck": "npm run typecheck --workspace @flash-sale/backend"
  },
  "engines": {
    "node": ">=18"
  }
}
